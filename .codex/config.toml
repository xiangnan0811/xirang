# Native project roles inherit the user's model and reasoning configuration.
# Keep implementation delegation bounded to the main session's direct children.
project_doc_fallback_filenames = ["AGENTS.md"]

[agents]
max_depth = 1
