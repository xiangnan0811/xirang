# Independent review 2026-08-21 (post v0.50.1)

Source: Alan-commissioned full review of `main` / GitHub Release `v0.50.1` / Docker Hub `linnea7171/xirang:0.50.1`. No production click-through. No repo edits by that reviewer.

This file is the Child 17 findings ledger source. Do not copy it onto the parent.

## Verdict (unchanged until Child 17 evidence)

| Gate | Result |
|---|---|
| Wide GA / default-on | No-Go |
| Parent archive | No-Go |
| Controlled Core-only pilot | Blocked by leftover snapshot HTTP bypass until that P0 is closed |

## Findings Child 17 must close

### P0 — leftover snapshot API bypass

Still registered in `backend/internal/api/router.go` 765–769:

- `GET /tasks/:id/snapshots`
- `GET /tasks/:id/snapshots/:sid/files`
- `GET /tasks/:id/snapshots/search`
- `GET /tasks/:id/snapshots/diff`

All use `RBAC("tasks:read")` + `OwnershipTaskCheck`. Viewer has `tasks:read`. Search takes `?q=` and returns `snapshot_id`, full paths, size, mtime (up to 200). No secret classification, no secret-reveal proof, no backup-asset search audit.

Preferred fix: remove or return **410 Gone**. Catalog / Search / Diff go through the new planes.

`POST /tasks/:id/snapshots/:sid/restore` is Admin + step-up + credential grant. Not the same Viewer leak, but a second recovery plane.

### P1 — enablement is not atomic

`TransitionFeature(true)` (`backend/internal/backupasset/runtime/runtime.go` 1940–1969) authorizes, prepares content, sets admission/content ready. Search rekey / search ready / Worker start only happen in `StartupPass`.

Handler `Enabled` (`backend/internal/api/router.go` 176–199) still follows requested `SearchOverlayConfig().Enabled`, not `Runtime.FeatureLive()`.

Hot enable → settings look on, search 503 until restart.

### P1 — parent lacks real final-acceptance evidence

Parent staying `planning` is correct. Need a bound acceptance protocol: SHA, image digest, DB engine, Provider mode, browser, acceptor, failures/waivers, rollback drill. PR #444 test plan was never checked off. Formal release is **v0.50.1**, not v0.50.0.

### P1 — CI gates are soft

`govulncheck`, `npm audit`, coverage, Codecov can fail green (`continue-on-error: true` in `.github/workflows/ci.yml`). No `go test -race`. Frontend is Vitest/jsdom only. Need race on core packages, real-browser E2E, unhandled MSW fail, non-allowlisted console fail, vuln block threshold, coverage floor on backup-asset packages.

### P1 — scale / cloud / failure not certified

`scripts/test-backup-asset-load.sh` refuses million-entry / zip-bomb / SIGKILL. Rclone Native AWS remains `not_executed` — must not enter the supported matrix until a live suite runs.

### P1 — search audit fail-open

`BackupAssetSearchHandler.writeAudit` (`backup_asset_search_handler.go` 267–273) logs a warning and still returns results, including secret search. No durable outbox exists in `backend/internal`.

### P2 — F7 preview renew + real-browser polish

`renewPreview` → `loadPreview` → `issueContentTicket(..., { revealOnce: true })` in `web/src/features/backup-assets/use-backup-assets-state.ts` 1285–1306. Ticket expiry re-prompts Admin TOTP. Preview pane is 18rem. Restic Range is unproven.

### P2 — risk ledger / doc truth

One authoritative ledger before parent archive. Parent notes still say formal release is v0.50.0.

## Standing locks (do not reopen)

- Parent stays `planning` until Alan independently Go after production evidence.
- `backup_assets.enabled` CodeDefault stays `"false"`.
- Do not publish Worker / do not edit `publish-images.yml`.
- Review conclusions stay on this child, not the parent.
- Wide GA / default-on stays No-Go.
