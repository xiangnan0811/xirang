> **Current disposition (2026-09-16):** see [reconciliation.md](reconciliation.md). Older status, delivery gates and deferred-work ownership below are historical where that record supersedes them.

# Execution plan

1. Completed: read external report; four parallel read-only scoped audits; main source adjudication. All R8-01 through R8-08 accepted. User selected full R8-06 isolation with remote helper allowed.
2. Implement four file-disjoint backend work packages: task editing/scheduling (01/04/05); authentication and volume discovery (02/08); Restic semantics and secret transport (03/07); rsync confinement (06). Preserve all prior-round fixes. Workers run targeted regression scenarios and report precise evidence.
3. Integrate frontend safe settings/presence/revision/manual scheduling and truthful Restic wording; discovery AbortController. Generate API documentation and run browser acceptance on isolated services.
4. Run full relevant backend/frontend gates plus PostgreSQL concurrency and disposable SSH/Rsync integration. Test deployed helper missing/unsupported behavior, remote invocation and root/link replacement rather than only strings.
5. Freeze stable diff for independent integration-reviewer GPT and grok-reviewer Grok discovery. Main maintains one findings ledger. At most two batch repair/verification cycles; no sibling finding leakage before first passes finish.
6. After smoke proof, update affected existing docs/changelog and remove throwaway resources. Readiness only with all confirmed findings closed and required gates passing; otherwise explicit NOT READY evidence. No release requested.

## Baseline / rollback
Work branch fix/v05512-review from baseline 180bc2cc1c69f05317c9e224d373ab57aa3a5d98. Do not revert working-tree files wholesale or restore old task-row compensation. No production data touched.

## Independent review ledger — discovery 1
Both independent lanes completed on intended models: GPT openai-codex/gpt-5.6-sol:high; Grok xai-oauth/grok-4.6:xhigh. Frozen baseline HEAD remained 180bc2cc; outputs agent://r8-gpt-discovery-1 and agent://r8-grok-discovery-1. No writes or sibling finding disclosure during first passes.

| ID | Sources | Confirmed root cause | Status |
|---|---|---|---|
| RG-01 | GPT-R8-01, Grok F3 | Confined managed hardlink publication lacks pinned/read/REFER parent operand | original defect closed by repeat discovery; distinct topology issue RG-11 |
| RG-02 | GPT-R8-02 | Restore-target SSH verification uses source role | closed by explicit role and actual local/SSH eight-layout proof; repeat discovery agrees |
| RG-03 | GPT-R8-03 | Policy sync erases explicit manual schedule | closed by migration88 provenance and policy lifecycle/restart proof |
| RG-04 | GPT-R8-04 | ABI2 advertised despite TRUNCATE requiring3 | closed by ABI3 minimum and capability tests |
| RG-05 | Grok F1 | UpdateTask response revision precedes schedule sync | closed by durable reload and next-write test |
| RG-06 | Grok F2 | Realtime handshake omits durable revocation | closed by bounded check before logs/SSH and two-Core proof |
| RG-07 | Main full gate | Concurrent terminal revocation SQLite fixture locks | closed by WAL fixture; full backend handlers pass |
| RG-08 | Main repair inspection | Public cleanup accepts arbitrary deletion root | closed by authenticated descriptor-bound cleanup; distinct cancellation issue RG-12 |
| RG-09 | Main repair inspection | Namespace reexec accepts untyped differing FDs | closed by NS_GET_NSTYPE checks and forged-marker test |
| RG-10 | GPT-R8-RD2-01 | Capture-kind probe and target evidence bypass descriptor boundary | original defect closed by both final verification lanes; distinct root-order issue RG-14 |
| RG-11 | GPT-R8-RD2-02 | Parent hardlink reuse overwrites source inode-group topology | live-publication defect closed by both final verification lanes; distinct reconcile issue RG-13 |
| RG-12 | GPT-R8-RD2-03 | Parent-death SIGKILL prevents alias cleanup on cancellation | closed by both final lanes and actual forced-kill cleanup proof |
| RG-13 | GPT verification N1 | Committed hardlink reconcile retains scalar-only eligibility and rejects valid topology transitions | CLOSED: authenticated commit/final/parent evidence retained; actual break/create reconciliation and tamper rejection PASS; both lanes agree |
| RG-14 | GPT verification N2 | Local kind probe stops at first lexical root, rejecting usable explicitly allowed nested symlink root | CLOSED: most-specific usable descriptor-root precedence; exact root/descendant, both orders, both layouts actual PASS; both final lanes agree |
| RG-15 | GPT verification N3, Grok residual | Unused LocalLinkDest/--link-dest privileged grammar remains after provider cutover | CLOSED: obsolete production API/grammar/FD/Landlock/alias paths removed; negative grammar PASS; both lanes agree |
| RG-16 | Main final lint | Test helper LookPath result assigned but unused at confinement_integration_test.go:502 | CLOSED: corrected test assignment; final full lint0 |

Batch ownership: Rsync helper/provider/capture/verifier (RG-01/02/04); task/model/migration/policy sync (RG-03/05); realtime auth and terminal fixture (RG-06/07). Main owns docs, evidence, adjudication. Durable schedule provenance materially changes persistence, so after this batch use the one permitted repeat discovery wave on the original reviewers, then only scoped verification for remaining repairs. Maximum two batched repair cycles still applies. Current readiness NOT READY.

Evidence before batch: frontend check 213 files/1976 tests+build/type/lint PASS; real browser rename/manual/409 draft/reopen PASS; two-Core ordinary logout PASS; actual bad-Cron startup isolation/manual restart/invalid PUT unchanged/fresh create revision/explicit excludes[] PASS; required PostgreSQL task script selected77 PASS. Main confinement local+SSH tests with built helper PASS (artifact777). Full lint0 issues. Full serial backend completed310s with only RG-07 failure; all other packages PASS. /tmp user quota false reds experimentally isolated (agent://r8-suite-environment-1), no updater changes warranted. Browser, Vite and disposable Core processes stopped; PostgreSQL test container still available.

Batch 1 evidence so far: RG-03/05 targeted policy/service/runner tests and SQLite migration suite PASS; required PostgreSQL migration tests PASS, main task PostgreSQL script selected77 PASS. Actual Core HTTP confirms explicit manual survives policy rename/disable/resume and process restart; inherited schedule tracks policy. RG-06/07 full handlers PASS, repeated20x concurrency/auth PASS; actual two-Core logs socket accepts a fresh session, rejects it after sibling logout with1008, and preserves unrelated session. Main regenerated Swagger after task schema/realtime changes. Namespace-capability probe in default Alpine Docker returns EPERM; deployment docs explicitly describe fail-closed limitation without privilege escalation. RG-01/02/04 still awaiting final helper namespace lifecycle and local/SSH proof; no repeat discovery used yet.

Repeat discovery completed on original GPT and Grok identities/intended models, no fallback, with frozen tracked snapshot `repeat-discovery-tracked.diff` plus untracked implementation. Grok found no new defects; GPT reported three material findings retained by main (RG-10/11/12). GPT corrected a baseline transcription error by explicit message: actual inspected supplied frozen tree/snapshot is against 180bc2cc1c69f05317c9e224d373ab57aa3a5d98. The one permitted repeat discovery is consumed. Final batched repair cycle2 now active; afterward only original-reviewer scoped verification, no new broad review.

Batch1 final gates: full serial backend336s passes all packages except unchanged updater320MiB test under known /tmp UID quota pressure. Split updater acceptance passes: 320MiB test on disk-backed temp, all remaining updater tests on /tmp (long disk paths exceed Unix socket limit). No updater source changes. Full lint has exactly one ineffectual aliasPath initialization queued in batch2. Actual helper/local+SSH/capture/restore8layouts and managed hardlink proof pass; pinned image util-linux mount/unshare packages install successfully. Current readiness remains NOT READY for RG-10/11/12 and lint.

## Final acceptance — NOT READY
Both original reviewers completed final `review_mode: verification` on intended GPT/Grok models, with no sibling findings disclosed during their passes. They agree RG-10/11/12 original defects are resolved. Grok returned pass; GPT retained three concrete repair consequences. Main preserves the valid minority findings as RG-13/14/15. Two batched repair cycles and the one repeat discovery are consumed: no batch3, no additional agents/review wave, no further implementation or lint edit. Existing branch changes are preserved but MUST NOT be treated as release-ready.

RG-13: `rsync_fidelity.go:88-125` committed-candidate validator still requires each scalar-equivalent path to share its parent inode; `rsync_publication.go:854-887` calls it during reconcile. Valid break/create-link commits now deliberately differ in topology, so interruption recovery can reject a correctly committed point. Required continuation: reconcile against authenticated committed manifest/marker evidence after source-aware precommit validation, or authenticated source topology evidence; cover both topology-transition reconciliations.

RG-14: `rsync_capture.go:855-885` returns the first lexical root before proving the requested relative object is accessible. Roots `/allowed`, `/allowed/link` with an explicitly permitted `/allowed/link -> /outside` reject `/allowed/link/file` in one order and accept in reverse. Required continuation: try usable descriptor-rooted candidates without weakening confinement; test both orders.

RG-15: remove callerless LocalLinkDest, accepted --link-dest/helper mount/FD grammar and its Landlock/alias/rewrite branches/tests. RG-16: final lint reports one test-only ineffectual LookPath assignment; left unchanged at the repair limit.

Final backend evidence: `go test -p 1 ./... -skip ^TestServiceStreams320MiBCanonicalBundleBelowHeapBudget$ -count=1` PASS355.35s; the excluded unchanged quota-sensitive case separately PASS2.235s on short disk-backed temp. This is full split-suite coverage, not a single all-tests invocation. Full lint FAIL exactly RG-16. Frontend full check remains 213 files/1976 tests plus type/lint/build PASS, actual browser scenarios unchanged. Final actual helper/local+SSH capture escape/role tests (artifact1338), eight-layout restore verifier (artifact1339), forced-kill/negative cleanup and pinned replacement tests PASS. Configured and unconfigured actual hardlink break/create transitions PASS; no reconciliation transition coverage yet. PostgreSQL task77 and SQLite/PostgreSQL migration88 evidence remains current (batch2 touched neither).

No commit, push, release, version bump, production access, or real backup deletion. Original user report and requested plan preserved. Detailed execution logs/review snapshots retained under `/home/murray/.cache/xirang-r8-gates-wo0dpmf1` as evidence; disposable binaries/databases/container are cleanup-only, not deliverables.

Cleanup completed: stopped the owned disposable PostgreSQL container (`--rm`); removed owned Core/helper binaries, fake HTTP SQLite databases/WAL/SHM, temporary SSH-key/tool directories and short disk-backed test directory. Core/Vite/browser processes were already stopped. Kept logs, review diff snapshots, browser screenshots and original report/plan. No unrelated services, Docker images or directories removed.

## User-authorized blocker continuation
User explicitly requested continued work on the final blockers after the NOT READY handoff. Scope is RG-13 through RG-16 only; prior closed findings and original evidence remain intact. Main authorizes one bounded continuation batch, with at most one corrective verification cycle if needed, not an unrestricted reopening of the original audit. Original GPT/Grok identities will perform `review_mode: verification` only on repair diffs, direct consumers and relevant tests; no new discovery identities/wave.

Ownership: provider worker repairs RG-13 committed reconciliation with authenticated marker/final-manifest and parent immutability guarantees, adding break/create topology interruption recovery coverage; helper/executor worker repairs RG-14 usable descriptor-root selection across overlapping roots, removes all RG-15 dead link-dest contract paths, and fixes RG-16 test lint. No persistence-format change, namespace redesign, reintroduction of provider --link-dest, production access, commit or release. Main integrates evidence, runs final backend/lint gates and updates this ledger. Status remains NOT READY until these four findings close.

Continuation verification1: both original intended-model lanes completed independently. RG-13/15/16 closed by both; no persistence/digest formula changed. Main backend split-suite coverage PASS (serial357.79s + disk-backed320MiB2.153s), full lint0. RG-14 descendant probing is repaired, but GPT retained the original root-order finding for the exact configured symlink root itself: broad-root Lstat reports symlink while exact-root Lstat(.) reports directory. Main accepts; corrective continuation cycle2 is limited to most-specific usable configured-root precedence and exact-root/trailing-slash capture regressions. No new broad audit or reopened closed findings.

## Current acceptance — READY for requested fixes
User-authorized continuation completed. All confirmed RG-01 through RG-16 are closed; prior NOT READY sections above are historical evidence, not current status. Final RG-14-only verification passed independently on original GPT `openai-codex/gpt-5.6-sol` and Grok `xai-oauth/grok-4.6`, no fallback and no new material findings. RG-13/15/16 closures from the preceding verification remain valid. No new broad discovery or persistence format change.

Final evidence: continuation full serial backend (excluding only unchanged quota-sensitive320MiB test) PASS357.79s, separately that case PASS2.153s on disk-backed temporary storage. After the final two executor-file correction, main re-ran full helper/executor/verifier/provider packages PASS (0.301/11.666/25.848/1.680s) and full golangci-lint0. Actual exact configured symlink-root capture tests cover both root orders and trailing/non-trailing layouts with payload evidence (artifact1420, no skips); final helper1425, actual executor local/SSH1426 and eight-layout restore verifier1427 PASS. Frontend/API/migration behavior unchanged in this continuation, earlier evidence retained.

Rsync reconciliation trusts existing authenticated commit/final-manifest and parent evidence after live source-aware validation. Manifest format still omits device/inode/nlink; this change does not introduce a new postcommit topology-only tamper-attestation guarantee. Deployment capability fail-closed requirements remain unchanged. Readiness here means requested repair acceptance, not a release: no commit, push, version bump, production deployment or real backup mutation.

Continuation cleanup completed: removed rebuilt `/tmp/xirang-rsync-confined` and owned `/home/murray/r8-QZ6rqA` test storage after all runtime/reviewer checks. No temporary service/container was started in this continuation. Logs and review snapshots remain preserved as evidence.
