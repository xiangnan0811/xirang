# Trellis 0.6.14 native/auto Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> `superpowers:executing-plans` to implement this plan task-by-task. Steps use
> checkbox syntax for tracking.

**Goal:** Safely upgrade Xirang's project Trellis runtime to 0.6.14 and enable
native Codex auto dispatch without losing project customization or task memory.

**Architecture:** Use the updater's `--create-new` conflict boundary, audit every
managed change, then merge Xirang's explicit values into the 0.6.14 templates.
Verify the result at CLI, configuration, hook, task, Git, and protected-file
levels before completing the repository PR workflow.

**Tech Stack:** Trellis CLI, YAML/JSON/TOML, Python hooks, Git, GitHub CLI.

---

## 1. Prepare And Activate The Maintenance Task

- [x] Confirm `HEAD`, `main`, and `origin/main` are
  `0d501d070afd349131c2fc77c10cd4314580add8`.
- [x] Create `codex/trellis-0.6.14-upgrade` from synchronized `main`.
- [x] Create independent task `08-10-trellis-0-6-14-native-auto-upgrade`.
- [x] Record the protected dirty paths and SHA-256 values.
- [x] Validate the planning artifacts and self-review them against the approved
  native/auto design.
- [x] Start this maintenance task so its status becomes `in_progress`.

## 2. Upgrade Managed Templates

- [x] Confirm `trellis update --help` documents `--create-new`, then run
  `trellis update --create-new`; never pass `--force`.
- [x] Capture the complete changed/untracked file inventory immediately after
  update.
- [x] Review every `.new` sidecar and classify it as generated replacement,
  Xirang customization merge, or obsolete artifact.
- [x] Confirm `.trellis/tasks`, `.trellis/spec`, `.trellis/workspace`, and
  `.trellis/.developer` retain project data.

## 3. Merge Project And Codex Configuration

- [x] Keep the generated 0.6.14 config comments while preserving Xirang's
  journal and channel values; set `codex.dispatch_mode: auto`.
- [x] Keep the bundled native workflow and its human planning/start/check/commit
  gates.
- [x] Register worktree-safe `UserPromptSubmit` and matched `SubagentStart`
  commands in `.codex/hooks.json`.
- [x] Confirm `inject-subagent-context.py` and all three native Trellis agent
  definitions exist; pin project agent recursion depth to 1.
- [x] Add the journal-only union merge rule in `.gitattributes`.
- [x] Resolve all `.new` sidecars and audit `.trellis/.template-hashes.json`,
  including removal of the unmodified typoed `trellis-spec-bootstarp` skills.

## 4. Verify The Upgrade

- [x] Check `trellis --version`, `.trellis/.version`, `trellis platforms`, and a
  final `trellis update --dry-run`.
- [x] Compile all Python files under `.trellis/scripts`, `.codex/hooks`, and
  `.claude/hooks`, then remove generated bytecode artifacts.
- [x] Parse YAML, JSON, and TOML configuration using structured parsers.
- [x] Simulate `UserPromptSubmit` and all three matched `SubagentStart` events;
  verify valid JSON and active-task context.
- [x] Run task list, current/source, and validate checks; confirm Task 7 stays
  `in_progress` and Task 8 remains absent.
- [x] Search for unresolved `.new` files and run `git diff --check`.
- [x] Recheck protected hashes and ensure the protected paths are untracked and
  unstaged.
- [x] Run `trellis-check` inline and address all findings; `make check` passes.

## 5. Finish Through The Repository Workflow

- [x] Review whether any project spec needs updating; no update is needed because
  no application executable contract changed and Trellis's managed meta docs
  already describe the new platform behavior.
- [ ] Stage only reviewed maintenance-task paths and create a conventional commit.
- [ ] Push the topic branch and create a PR targeting `main`.
- [ ] Monitor every required CI job; fix and push failures on this branch until
  all required checks pass.
- [ ] Squash merge the PR, then monitor Release Please and applicable post-merge
  workflows; record when no formal release or Docker publish is expected.
- [ ] Sync local `main` with `origin/main`, remove the local topic branch, archive
  the maintenance task according to Trellis workflow, and verify a clean final
  state except for the two protected historical untracked paths.
- [ ] Do not start Task 8.
